<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.test.dao.PayRcvSumDao"> 
<select id="lst" parameterType="PayRcvSumVo" resultType="PayRcvSumVo">
SELECT A.RCV	RCV
     , B.PAY	PAY
  FROM
		(
		SELECT IFNULL(SUM(AMT),0) RCV
		  FROM evnt_asset
		 WHERE EVNT = 'E0002'
		   AND WK_DT BETWEEN #{FROM_DT} AND #{TO_DT}
		   
		) A JOIN
		(
		SELECT IFNULL(SUM(AMT),0) PAY
		  FROM evnt_asset
		 WHERE EVNT = 'E0001'
		   AND WK_DT BETWEEN #{FROM_DT} AND #{TO_DT}
		) B
WHERE 1 = 1
</select> 
</mapper>